<!--components/w-tab.wxml-->
<view class="tab">
  <block wx:for="{{titles}}" wx:key="{{index}}">
    <view class="tab-item {{currindex==index?'active':''}}" bindtap="itemclock" data-index="{{index}}">
    <text>{{item}}</text></view>
  </block>
</view>